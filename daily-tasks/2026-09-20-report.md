# 일일 작업 보고 — 2026-09-20

담당: master (총괄/검수)

## 1. 착수 전 저장소 상태 점검

- 니치(주제)는 계속 확정 상태(`README.md`, `planning/content-plan.md`): 일본 지역별
  "가볼만한 곳 + 맛집" 여행 정보 사이트. 오늘 세션에서 니치 재정의는 없었다.
- 세션 시작 시 `HEAD detached from refs/heads/main` 상태였다. 다만 어제(09-19)와 달리
  실제 데이터 유실 위험은 없었다: 로컬 `main` 브랜치 포인터가 09-16 커밋에 멈춰 있었던
  것은 로컬 클론이 `git fetch` 전 캐시 상태였기 때문이고, `git fetch origin main` 실행
  결과 원격 `origin/main`은 이미 09-19 보고 커밋(`0c37fe0`)까지 정상 반영되어 있었다.
  `git checkout -B main origin/main`으로 로컬 브랜치만 원격과 동기화했다(히스토리 변경·
  강제 push 없음).
- 지난 보고서(특히 09-19), 사이트 코드, `planning/content-plan.md`, `support/faq.md`를
  대조해 오늘 처리 가능한 **새로운** 항목을 직접 코드 레벨에서 찾았다(이미 결함 없음이
  확인된 스킵 링크·기존 대비 감사 항목은 반복하지 않음):
  1. `content/ureshino-onsen/index.html`의 히어로 이미지가 `fukuoka.jpg`를 규슈 권역
     대표 이미지로 재사용하는 것 자체는 `naoshima-day-trip`이 `setouchi-hokuriku.jpg`를
     쓰는 것과 같은 정상 패턴이지만, alt 텍스트("후쿠오카 수변 도시 풍경")가 우레시노
     온천 글 내용과 맞지 않는 지명/장면을 스크린리더 사용자에게 전달하고 있음을 발견 →
     개발팀에 지시.
  2. 09-19 색상 대비 감사가 정적 텍스트/푸터 위주였고, 인터랙티브 컴포넌트(필터 버튼
     `.filter-pill`, 지도 링크 `.spot-map-link`)는 다루지 않았음을 확인 → 디자인팀에
     새로 감사 지시.
  3. `assets/js/main.js`의 `buildSpotMapQuery`/`initSpotMapLinks`를 직접 읽어, "📍 지도에서
     보기" 링크가 정확한 좌표가 아니라 이름 기반 구글맵 검색이라는 실제 동작을 확인했는데
     이를 안내하는 FAQ 항목이 없었음 → CS팀에 신규 항목 작성 지시.
  4. 다음 콘텐츠(가나자와) 본문 작성은 실제 스팟 리서치(WebSearch)가 필요한 기획팀
     (planner) 영역이라 오늘도 지시 대상(개발/디자인/CS)에서 제외했다.
  5. `<img>` 태그에 `width`/`height` 속성이 없는 점도 살펴봤으나, CSS에서 `.post-hero-image`
     (`aspect-ratio: 16/9`)와 `.region-card-media`(`aspect-ratio: 4/3`)로 이미 레이아웃
     시프트(CLS)를 방지하고 있음을 확인해 — 실제로는 문제가 아니라고 판단, 팀에 지시하지
     않았다(불필요한 작업을 만들지 않기 위해 착수 전에 걸러냄).

## 2. 팀별 작업 및 검수 결과

### 개발팀 (developer)
**지시 내용**: `content/ureshino-onsen/index.html` 히어로 이미지의 alt 텍스트를,
naoshima 사례처럼 장소를 과도하게 특정하지 않는 정직한 일반 표현으로 수정. src/class/
loading 등 다른 속성과 다른 파일은 건드리지 말 것.

**산출물**: alt 값을 "후쿠오카 수변 도시 풍경" → "규슈 지역 이미지"로 변경. `region/fukuoka/
index.html`(실제 후쿠오카 허브 페이지라 원래 alt가 정확함)은 그대로 유지.

**검수**:
- ✅ **마스터가 직접 재확인**: `git diff content/ureshino-onsen/index.html` 결과 변경이
  정확히 alt 값 한 곳(1줄)뿐임을 확인, `git status --porcelain`으로 다른 파일 무변경도
  확인.
- ✅ 요청 범위를 정확히 수행, 이미지 파일·구조·다른 텍스트 무변경.
- ✅ 커밋/배포 시도 없음(지시대로 master가 검수 후 커밋).

### 디자인팀 (designer)
**지시 내용**: 09-19 감사에서 빠진 인터랙티브 컴포넌트(`.filter-pill` 기본/hover/
focus-visible/active, `.spot-map-link`)의 라이트·다크 모드 색상 대비를 WCAG AA(4.5:1)
기준으로 직접 계산해 점검하고, 실제로 미달인 경우에만 CSS 변수 값을 조정.

**산출물**:
- 위 컴포넌트의 실제 CSS 변수 hex 값을 확인하고 상대 휘도 공식을 손으로 단계별 계산
  (해당 세션에 Bash 도구가 없어 알려진 참조값 Tailwind teal-700/blue-700과 대조해 계산
  신뢰도 확인).
- 결과: 모든 조합이 라이트 5.21:1 이상, 다크 6.61:1 이상으로 AA 기준을 여유 있게 통과 →
  **CSS 변경 없음**(문제가 없어서 손대지 않았다고 정직하게 보고).

**검수 및 보완**:
- ✅ **마스터가 Python으로 WCAG 상대 휘도 공식을 독립 구현해 재계산**: `.filter-pill`
  기본(라이트 5.80:1/다크 7.60:1), hover(5.47:1/8.56:1), focus-visible outline(6.70:1/
  6.61:1), `.is-active`(teal쪽 라이트 5.21:1·다크 9.07:1, blue쪽 6.38:1·12.32:1),
  `.spot-map-link`(5.47:1/8.56:1) — 디자인팀 계산치와 소수점 둘째 자리까지 정확히 일치.
- ✅ `git status --porcelain`/`git diff --stat`으로 `assets/css/style.css`에 변경이 전혀
  없음을 직접 확인 — 디자인팀의 "변경 없음" 보고가 사실과 일치.
- ✅ 근거 CSS 변수(hex 값)도 파일에서 직접 grep해 디자인팀 보고와 일치함을 확인.

### CS팀 (support)
**지시 내용**: 기존 예시 FAQ 5개 항목을 오늘 실제 변경사항(개발팀 alt 텍스트 수정,
디자인팀 대비 재감사 결과)과 재대조해 필요한 곳만 갱신 + `main.js`의 실제 동작을
근거로 "지도에서 보기" 링크 정확도 관련 신규 예시 FAQ 항목 작성. 실제 발송 금지.

**산출물**:
- 5개 기존 항목 대조 결과 오늘 변경사항이 답변 실질 내용에 영향을 주지 않음을 확인 →
  `최종 수정일`만 09-20으로 갱신(항목 4의 본문 인라인 날짜 표기 1곳도 사실은 그대로 두고
  표기만 일치시킴).
- `assets/js/main.js`의 `buildSpotMapQuery`/`initSpotMapLinks`를 직접 읽고, "📍 지도에서
  보기"가 정확한 좌표/place ID가 아니라 이름 기반 구글맵 검색 링크라는 실제 코드 동작을
  근거로 신규 항목("...생각했던 곳이 아니라 다른 곳이 나와요") 작성. 정확한 좌표 제공을
  약속하지 않음.
- 상단 "현재 상태 안내" 블록을 오늘 실제 결과로 갱신(어제자 세부 내용은 제외하고 오늘
  있었던 일만 서술).

**검수**:
- ✅ **마스터가 직접 재확인**: `git diff support/faq.md` 결과 변경 범위가 보고 내용과
  정확히 일치(상태 안내 블록 갱신, 날짜 5곳+인라인 1곳, 신규 항목 1개 전체 추가, "실제
  문의" 빈 상태 각주 날짜 갱신)함을 확인. 기존 답변 본문(신규 항목 제외)은 무변경.
- ✅ 개인정보 신규 기록 없음, 실제 발송/등록 없음, 근거 없는 좌표 제공 약속 없음.
- ✅ CS팀도 Bash 도구가 없어 자체 `git diff` 확인은 못했다고 보고 → master가 대신 확인.

### master 직접 처리
- 세션 시작 시 로컬 `main` 브랜치가 원격보다 뒤처져 보였던 원인을 `git fetch`로 규명
  (실제 데이터 유실 없음, 단순 로컬 캐시 지연).
- 착수 전 `<img>` width/height 부재를 검토했으나 CSS `aspect-ratio`로 이미 해결되어
  있음을 확인해 불필요한 작업 지시를 걸러냄.
- 디자인팀의 대비 계산(WCAG 상대 휘도 공식)을 독립 Python 스크립트로 재실행해 일치 확인.
- 개발팀·CS팀 산출물을 `git diff`로 직접 대조해 보고 범위와 실제 변경이 정확히 일치하는지
  확인(두 팀 모두 Bash 도구가 없어 자체 diff 확인 불가 → master가 대신 수행).
- 사이트 전체 내부 링크/자산 참조 무결성 재검사(Python 스크립트, 14개 HTML의 모든 상대
  `href`/`src` 대조) — **깨진 링크·참조 0건**.
- 세 팀 작업을 팀별로 분리해 2개 커밋(개발팀+CS팀; 디자인팀은 변경 없음)으로 나눠
  커밋·푸시.

## 3. 전체 범위 재확인 (master 직접 수행)

오늘 세션 중 실제로 저장소 파일이 변경된 곳은 정확히 다음과 같다: `content/
ureshino-onsen/index.html`(개발팀, alt 텍스트 1줄), `support/faq.md`(CS팀, 날짜 갱신
6곳 + 신규 항목 1개 + 상태 안내 블록). `assets/css/style.css`는 디자인팀이 점검했지만
문제가 없어 변경하지 않았다. 세 팀 모두 각자 보고한 범위만 정확히 변경했으며, `git diff`/
`git status --porcelain`으로 매 단계 master가 직접 대조해 확인했다.

## 4. 커밋/배포

- 개발팀(alt 텍스트 수정) → 커밋 `8728fd5` → `git push origin main` 완료.
- CS팀(FAQ 날짜 갱신·신규 항목) → 커밋 `9efe0f2` → `git push origin main` 완료.
- 디자인팀은 변경 사항이 없어 커밋 없음.
- master(이 보고서) → 이 커밋에 이어서 커밋·푸시 예정.
- Netlify/Vercel CLI 등 직접 배포는 전혀 수행하지 않았다. `git push origin main`으로만
  반영했다.

## 5. 승인 필요 / 보류 항목

- **다음 콘텐츠(가나자와) 본문 작성** — 실제 스팟 리서치(WebSearch)가 필요한 기획팀
  (planner) 영역이라 오늘도 진행하지 않았다.
- **`sitemap.xml`/`robots.txt`, 각 HTML의 `og:url`/`canonical`/`og:image`/
  `twitter:image`, JSON-LD의 `url`/`@id` 필드** — 실제 배포 도메인이 확정되어야 활성화
  가능. 오늘도 사용자 판단 대기.
- **"예약 & 이동 정보"(제휴 링크) 미연동** — 실제 제휴 계약·외부 서비스 가입이 필요한
  사안이라 사용자 승인 없이는 진행할 수 없다. 오늘도 변경 없음.
- **우레시노 온천 전용 히어로 이미지 부재** — 09-12부터 반복 기록된 기존 이슈. 오늘 alt
  텍스트는 정확하게 고쳤지만, 이미지 자체(전용 사진)를 새로 구하는 것은 외부 리소스
  조달이 필요해 여전히 사용자 판단/향후 콘텐츠 제작 단계 소관으로 남긴다.
- **"📍 지도에서 보기" 링크가 이름 기반 검색이라 동명 매장/지명 시 부정확할 수 있는 점** —
  오늘 CS팀이 FAQ로 안내는 추가했지만, 근본적으로 정확한 좌표/place ID를 넣으려면 스팟별
  실제 위치 조사(기획팀 영역)나 별도 데이터 작업이 필요해 오늘은 안내에 그쳤다. 개선
  필요 여부는 사용자 판단 대상으로 남긴다.
- **실제 배포(호스팅 연동) 여부** — 이번 세션 도구로는 확인할 수단이 없다.
- **광고/제휴 실제 진행 여부·조건** — 사용자 판단이 필요한 항목으로 계속 남겨둔다.
- 오늘 되돌리기 어려운 작업(강제 push, 파일/브랜치 삭제, 실제 메시지 발송, 외부 서비스
  가입/결제 등)은 전혀 수행하지 않았다. 세션 시작 시 처리한 `main` 재정렬은 히스토리를
  새로 만들거나 덮어쓰지 않았고, 원격은 이미 정상 상태였다.

## 6. 참고 — 검증됨 vs 검증되지 않음 요약

- **실측/계산으로 검증됨**: `content/ureshino-onsen/index.html`의 변경이 alt 값 한 줄뿐임
  (master가 `git diff`로 직접 확인); `.filter-pill`(기본/hover/focus-visible/active)과
  `.spot-map-link`의 라이트·다크 대비율이 전부 AA 기준(4.5:1)을 5.21:1 이상 여유로 통과함
  (master가 WCAG 상대 휘도 공식을 Python으로 독립 구현해 재계산, 디자인팀 수기 계산과
  일치); `assets/css/style.css`에 실제로 변경이 없었음(`git status`로 확인); `support/
  faq.md`의 변경 범위가 보고와 정확히 일치하고 신규 FAQ 항목의 근거(main.js 코드 동작)가
  사실과 일치함(master가 diff와 코드를 직접 대조); 사이트 내부 링크/자산 참조 전수 검사
  결과 깨진 링크·참조 0건(14개 HTML 전 페이지 스캔); `<img>` width/height 부재가 실제로는
  CSS `aspect-ratio`로 이미 방지되어 있어 조치가 불필요함(CSS 직접 확인).
- **검증되지 않음(정직하게 미확인으로 남김)**: 색상 대비 계산은 hex 값 기반 수식 계산이며
  실제 브라우저 렌더링(안티앨리어싱, 폰트 굵기에 따른 체감 대비, axe/Lighthouse 같은
  자동화 도구)으로는 확인하지 못했음; 실제 스크린리더(NVDA/VoiceOver 등) 테스트(환경
  제약); "📍 지도에서 보기" 링크가 실제 구글맵에서 어떤 결과를 보여주는지는 네트워크
  접근 없이 확인 불가(코드 로직만 검증); 실제 배포 도메인·인터넷 공개 여부; 실제 방문자
  문의 채널 확인(이 세션 도구로는 접근 불가).
